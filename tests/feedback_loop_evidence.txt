======================================================================
FEEDBACK LOOP EVIDENCE: Does it produce better vocabulary?
Scenario: recordings from an ashram, spiritual teachings, kirtan sessions
======================================================================

── Step 1: Build simulated session history (5 sessions) ──────────────
Memory written. Sessions simulated: 5

Extracted feedback signal:
  Useful words (appeared in transcript/corrections): ['Anandamayi', 'Om Namah Shivaya', 'kirtan', 'mantra', 'satsang']
  Noise words (appeared in neither):                 ['bhajan', 'pranayama']

── Step 2: Worker guidance comparison ────────────────────────────────

WITHOUT feedback history (session 1, cold start):
  (no vocabulary guidance — no feedback history)

WITH feedback history (session 6+):
  Previously effective vocabulary (prioritize similar terms): Anandamayi, Om Namah Shivaya, kirtan, mantra, satsang
  Previously ineffective vocabulary (deprioritize): bhajan, pranayama

── Step 3: Guidance correctness verification ─────────────────────────
  PASS: 'om namah shivaya' correctly in useful list (priming worked)
  PASS: 'mantra' correctly in useful list (priming worked)
  PASS: 'satsang' correctly in useful list (priming worked)
  PASS: 'kirtan' correctly in useful list (priming worked)
  PASS: 'anandamayi' correctly in useful list (priming worked)
  PASS: 'pranayama' correctly in noise list (never appeared)
  PASS: 'bhajan' correctly in noise list (never appeared)
  PASS: No word appears in both lists (classification is consistent)

Guidance correctness: PASS

── Step 4: LLM vocabulary comparison (requires API) ──────────────────
LLM unavailable: No API endpoint configured
Skipping live generation comparison.

DETERMINISTIC EVIDENCE (no LLM required):
The guidance extraction logic is correct (Step 3 above).
When the Worker receives this guidance, it will:
  - See useful vocabulary: Anandamayi, Om Namah Shivaya, kirtan, mantra, satsang
  - See noise to avoid: bhajan, pranayama
Without feedback, the Worker generates cold with no history.
With feedback, it has signal about what actually helped.

── Step 5: Inverted signal regression ────────────────────────────────
Demonstrating the original bug and that the fix is correct.

ORIGINAL BUG: 'used' = word appeared as correction target only
  Scenario: kirtan appears correctly in transcript, no correction needed
  Old classification: kirtan → NOISE (Whisper got it right = no correction)
  Effect: system learns to deprioritize kirtan (WRONG)

FIXED SIGNAL: 'used' = word appears in transcript OR correction target
  Scenario: kirtan appears correctly in transcript, no correction needed
  New classification: kirtan → USEFUL (transcript hit = priming worked)
  Effect: system learns to keep priming kirtan (CORRECT)

Concrete example:
  vocab=['kirtan', 'bhajan']
  transcript='Everyone joined in for kirtan and sang together.'
  corrections=[]

  OLD result: used=[], noise=['kirtan', 'bhajan']
    kirtan gets labeled NOISE even though Whisper transcribed it correctly!

  NEW result: used=['kirtan'], noise=['bhajan']
    kirtan is USEFUL (transcript hit). bhajan is NOISE (never appeared). Correct.

  Regression assertion: PASS

── Summary ───────────────────────────────────────────────────────────

Finding 1 (inverted signal): FIXED
  - Signal now comes from transcript presence, not correction targets only
  - Words Whisper transcribes correctly are classified as useful (win)
  - Only words in neither transcript nor corrections are noise

Finding 2 (prompt bloat): FIXED
  - Worker no longer receives full memory file
  - Feedback replaces raw history (3 lines of guidance, not 3 layers)
  - Feedback section capped at 30 entries

Finding 3 (no evidence): ADDRESSED
  - This script demonstrates the feedback loop produces correct signal
  - Guidance is verified against known useful/noise classification
  - Regression test proves old bug is fixed

Overall: PASS
